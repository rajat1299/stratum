# stratum-sdk

Python client for the current Stratum HTTP API (synchronous HTTPX).

Semantic search is intentionally unsupported until the backend provides the derived index; `SearchClient.semantic` raises `UnsupportedFeatureError`.

## Development

See the full README after the SDK implementation slice is complete.
